# Local Terraform directories where plugins and modules are downloaded
.terraform/

# Terraform state files (contain sensitive info)
*.tfstate
*.tfstate.*

# Terraform variable files (may contain secrets)
*.tfvars
*.tfvars.json

# Crash log files generated by Terraform
crash.log

# Override files (local overrides, not for version control)
*.override.tf
*.override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# Lock file for provider versions (optional to ignore, but often tracked)
.terraform.lock.hcl

# Terraform plan output files
*.tfplan
*.plan
